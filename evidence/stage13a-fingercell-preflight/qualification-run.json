{
  "determinism_levels": [
    "repeat_in_the_same_process",
    "fresh_objects_in_the_same_process",
    "fresh_process"
  ],
  "determinism_requirement": "the exact same native integer at all three levels",
  "failed_qualification_is_kept": true,
  "failure_semantics_rule": "a failure arrives as an exception, a status or an error code, and never as a pseudo-score; a score of 0 is a legitimate score wherever the matcher returns one",
  "fixture_sources": [
    "official vendor fingerprint samples shipped inside the delivered archive",
    "local non-SD300 fixtures generated by this project"
  ],
  "gate": "PAIR_SELF_DETERMINISM_FAILURES",
  "mandatory_failure_probe_count": 4,
  "mandatory_failure_probes": [
    {
      "cause": "malformed_image",
      "what_it_establishes": "a structurally broken image is refused rather than scored"
    },
    {
      "cause": "valid_image_without_fingerprint_structure",
      "what_it_establishes": "an image the extractor declines produces a status, not a number"
    },
    {
      "cause": "missing_or_invalid_input",
      "what_it_establishes": "an absent or invalid input is refused before any scoring"
    },
    {
      "cause": "invalid_matcher_or_template_invocation",
      "what_it_establishes": "a controlled invalid matcher call returns a structured error"
    }
  ],
  "max_scoring_comparisons": 20,
  "no_score_value_is_published": true,
  "optional_failure_probes": [
    {
      "cause": "license_unavailable",
      "what_it_establishes": "the licence refusal is reported as itself, where this can be provoked without altering, bypassing or resetting the trial"
    }
  ],
  "orientation_requirements": [
    "score(A, B) is produced under the frozen binding",
    "score(B, A) is produced as a separate comparison, for observation only",
    "whether the two agree is published as a finding",
    "no symmetry is required and none is assumed",
    "the frozen binding is applied to every pair regardless of the finding"
  ],
  "pair_role_binding": [
    {
      "api_role": "reference",
      "pair_side": "pair.left"
    },
    {
      "api_role": "candidate",
      "pair_side": "pair.right"
    }
  ],
  "record_binding_fields": [
    "archive_sha256",
    "product_revision",
    "binding_fingerprint",
    "runtime_closure_fingerprint",
    "driver_fingerprint",
    "settings_profile_fingerprint",
    "fixture_fingerprint",
    "platform"
  ],
  "refused_orientation_reductions": [
    "average",
    "max",
    "min",
    "sorting the two paths",
    "choosing whichever orientation scores higher"
  ],
  "required_passes": [
    {
      "pass_name": "ordinary",
      "what_it_is": "score(A, B) under the frozen role binding"
    },
    {
      "pass_name": "repeat_same_objects",
      "what_it_is": "score(A, B) again, same process, same objects"
    },
    {
      "pass_name": "fresh_objects_same_process",
      "what_it_is": "score(A, B) again, same process, newly constructed objects"
    },
    {
      "pass_name": "fresh_process",
      "what_it_is": "score(A, B) again, in a separate process"
    },
    {
      "pass_name": "reversed",
      "what_it_is": "score(B, A), for observation only"
    },
    {
      "pass_name": "self",
      "what_it_is": "SELF(A, A) from two independent extractions"
    }
  ],
  "run": null,
  "schema": "stage_13a_qualification_run_v1",
  "self_semantics_requirements": [
    "image A is loaded into a fresh image object and extracted to template A1",
    "image A is loaded again into a separate image object and extracted to A2",
    "Match(A1, A2) is the SELF comparison",
    "no template is reused, cached or shared between the two sides"
  ],
  "status": "ACTION_REQUIRED"
}
