"""fpbench — a reproducible benchmark harness for fingerprint recognition.

The harness owns the experiment; algorithms are plugged in behind adapters and
never decide what runs. See docs/architecture and docs/adr.
"""

__version__ = "0.1.0"
